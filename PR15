## Практическая работа №15: Интеграция мультимедиа (бинарное хранение изображений)

**Дата выполнения:** 2026-04-14  
**Цель:** Добавить возможность хранения изображений для основных бизнес-сущностей в бинарном формате (BLOB).

**Выполненные задачи:**
- Добавлено поле `image VARBINARY(MAX)` в таблицу `code_reviews`.
- Все записи заполнены тестовыми бинарными данными (PNG 1x1 пиксель).
- Обновлены файлы `database.sql` и `README.md`.
- Изменения зафиксированы в Git и загружены в ветку работы №13.

**Пример использования:**
```sql
-- Получение изображения
SELECT review_id, DATALENGTH(image) as image_size 
FROM code_reviews 
WHERE image IS NOT NULL;
